# The Life Doula School: Claude Code Instructions

## Project summary

This is the complete rebuild of https://thelifedoulaschool.co.uk. Becca (Rebecca Sarah) no longer offers 1:1 services; she has pivoted entirely to teaching a living philosophy called The Life Doula Bridge, themed "Bridging Heaven and Earth", a middle path blending spiritual and practical everyday living, inclusive of all faiths and none, never pushy. Nothing is sold on this site. Its purpose is to inspire, inform, and support her future course and YouTube channel.

The client is minimally available and delivers content non-linearly in waves over weeks; the site must always look complete and absorb content as it arrives. The first design may be rejected by the client or by David, so the architecture makes a full restyle nearly free: everything visual flows from design tokens. David manages all content himself via Claude Code. There is no CMS and there never will be. Hosting is Cloudflare Pages, fully static output, auto-build on push to `main`, preview deployments per branch.

**Positioning (from the client, 2026-07-16, paraphrased):** the Bridge and the Bridging Keys are the product of the site: a way to change your life and be happy. Everything else on the site is evidence for why you should use them. The frame is how to have faith in a new, modern way, without church and rules, supported by historical facts, science, and new ideas. She regards this as her life's work and is in no rush. Keep this hierarchy in mind when structuring pages and internal links: keys first, everything else supporting.

## Standing rules

- **Tokens only.** Never hard-code a colour, font family, or spacing value in a component or page. All visual values come from CSS custom properties defined in `src/styles/tokens.css` and the active theme in `src/styles/themes/`. Stylelint enforces this; do not weaken the rule.
- **Content lives in content collections** (`src/content/`), never inline in components or pages. A redesign must never touch a markdown file, and a content change must never touch a component.
- **UK English** throughout: content, metadata, and code comments that refer to content.
- **No en or em dashes anywhere.** Use commas, colons, semicolons, or rewrite the sentence.
- **SEO checklist on every page** (see §8 of the original brief, mirrored in `docs/seo-checklist.md`): unique title and description (schemas make them required; description 50 to 160 chars), canonical, full OG and Twitter set, correct JSON-LD, breadcrumbs on nested pages, semantic heading order.
- **Definition of done for any task**: `npm run format && npm run lint && npm run check && npm run build` all pass; SEO fields present; `docs/content-status.md` updated; changes reviewed against the token rule; preview deployment link provided to David.
- **No client-side JS without written justification** in a code comment. Current justified scripts: mobile nav toggle, YouTube facade activation.
- **Never use the client's raster images structurally** (heroes, backgrounds, cards). Her "spiritual postcard" images may only appear inline within article body content, framed as content artefacts. Her SmartArt diagrams are specifications only; rebuild as native SVG components with her wording verbatim.
- **Her supplied wording is sacrosanct.** When client copy arrives, replace placeholders verbatim; do not editorialise.
- Keep `docs/content-status.md` and `docs/redirects.md` current with every content change.
- **URL continuity**: no indexed URL may 404. Trailing slashes always. Any page removal requires a 301 in `public/_redirects` and an update to `docs/redirects.md`.
- Git and deploys are handled manually by David; never run git or deployment commands.

## Open questions register

Keep this table updated as answers arrive.

| #   | Open question                                                                                                                                                                                                                                                                                                                                                                                            | Current assumption in build                                                                                                                                            | Implication if answer differs                                                                                                                                                                               |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | ANSWERED 2026-07-16: domain stays `thelifedoulaschool.co.uk` ("OK if you think I shouldn't change it I won't"). "Ascending to Joy" remains an on-site theme                                                                                                                                                                                                                                              | Build targets `thelifedoulaschool.co.uk`; "Ascending to Joy" appears as strapline and `alternateName` in WebSite JSON-LD                                               | Closed. Reopen only if she changes her mind in writing                                                                                                                                                      |
| 2   | Book: ANSWERED. Current material received 2026-07-16: the book is retitled _Birthing Joy ~ Releasing Pain_ (Cycle One: Acceptance), part of a planned three; `/book/` page built from it                                                                                                                                                                                                                 | `/book/` is live (footer link, Book JSON-LD, Life Doula Wheel SVG); the seven old book/chapter URLs 301 to `/book/`; page framing awaits her sign-off                  | As further cycles, lessons, and videos arrive, extend the book page; if she wants chapter excerpts republished at their old URLs, remove those redirects and create posts                                   |
| 3   | Keep or drop the LUXLife / Parent and Baby Awards page? NOT ANSWERED in her 2026-07-16 reply; chase when convenient.                                                                                                                                                                                                                                                                                     | Dropped; old URL 301 to `/about/`                                                                                                                                      | If kept: fold into About as a section (preferred); update redirect map                                                                                                                                      |
| 4   | ANSWERED 2026-07-16: client confirmed there are no contact details or social media to publish for now ("Nobody needs to contact me without services anyway")                                                                                                                                                                                                                                             | Contact page explains there is deliberately no way to book or reach out; no email, phone, or socials anywhere on the site; `sameAs` empty                              | If she later wants a public email or channel links: add to Contact and Person `sameAs` only once she supplies them in writing                                                                               |
| 5   | YouTube channel: the old `@mylifedoula7` is gone (404 verified 2026-07-16). Client confirmed 2026-07-16 she is setting up a NEW channel "to reflect the new name" and will send it when ready. Which handle, and does "new name" affect open question 1?                                                                                                                                                 | `CHANNEL_ID` in `src/config/site.ts` is empty; all YouTube sections and subscribe CTAs hide gracefully; build never depends on the feed                                | When she sends the new channel: set `CHANNEL_ID` and `channelUrl` in `src/config/site.ts`, sections appear automatically; add to `sameAs`; revisit the Contact page line about the channel being on its way |
| 6   | Final approved copy for: philosophy intro, 8 key summaries, each key's "7 Ways", protocols, three core beliefs, About                                                                                                                                                                                                                                                                                    | Placeholder copy written by David/Claude in the client's register, tracked in `docs/content-status.md`; no client draft document was available at build time           | Replace verbatim on delivery; her exact wording is sacrosanct                                                                                                                                               |
| 7   | Diagrams: PARTIALLY ANSWERED 2026-07-16. Received her diagrams.pdf: the karma cycle set, "The illusion of Separation", "Seeing through the illusion", the wheel, and a REVISED wheel table (Destruction/Grief Counsellor etc., now live in book-wheel.json). Her covering note: she must check everything through before it counts as approved; these were sent "so you have an idea of what I am doing" | Wheel uses her latest wording, status draft-placeholder; separation/karma diagrams not yet built into any page (they belong with the Core Beliefs when her copy lands) | When she confirms content is checked: mark wheel client-approved; build the separation diagrams as SVG components on the relevant belief pages with her wording verbatim                                    |
| 8   | Logo: INTERIM RESOLVED 2026-07-16 (David). The site identity is now the Life Doula Wheel mark (her own design) + Marcellus wordmark: header mark, favicon, apple-touch-icon, and OG image all use it                                                                                                                                                                                                     | `WheelMark.astro`, `public/favicon.svg`, `public/apple-touch-icon.png`, `public/og-default.png` (regenerate via `npm run og:generate`)                                 | If Becca later supplies a different logo: swap WheelMark and the favicon, re-run og:generate                                                                                                                |

## Working with this repo

- `npm run dev` to develop, `npm run test:build` for build plus internal link check.
- Themes: swap the single `@import` in `src/styles/theme.css` between `themes/dawn.css`, `themes/dusk.css`, `themes/garden.css` to restyle the whole site for a demo.
- `npm run contrast:audit` verifies WCAG AA for every token pairing in every theme.
- `npm run og:generate` regenerates `public/og-default.png` from the active theme tokens.
- YouTube data: `src/lib/youtube.ts` fetches the channel RSS at build; last successful data is cached in `src/data/latest-videos.json` (committed). Feed failure never breaks the build.
- **Lockfile rule**: Cloudflare Pages installs with npm 10, which rejects lockfiles written by npm 11. Add or update dependencies via `npx npm@10 install <package>` (never plain `npm install` while local npm is 11), or set `NPM_VERSION = 11` in the Pages build environment and regenerate the lockfile with npm 11. After any lockfile change, verify with `npx npm@10 ci` before pushing.
